import React from 'react';

const Streak = ({ streak }) => <div>
                                 <p>
                                   Streak:
                                   {streak}
                                 </p>
                               </div>;
export default Streak;
